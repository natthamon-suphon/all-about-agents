{
  "schemaVersion": 1,
  "id": "checkpoint",
  "description": "Persist bounded workflow checkpoint metadata so interrupted work can resume safely.",
  "failureMode": "fail-open",
  "storage": "durable-workflow",
  "recordedFields": ["timestamp", "taskId", "state", "status"]
}
