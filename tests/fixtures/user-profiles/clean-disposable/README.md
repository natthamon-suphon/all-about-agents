# Clean disposable user profile

This directory is a metadata-only fixture for T049 clean-profile checks. It
contains no credentials, vendor state, prompts, or live user files. Tests create
the actual profile in a temporary directory and remove it after each run.
