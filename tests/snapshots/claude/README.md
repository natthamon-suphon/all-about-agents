# Claude snapshots

Deterministic portable and template render snapshots live in this directory.

The snapshots include the rendered `hooks/bootstrap.mjs` startup handler and its native hook configuration.
