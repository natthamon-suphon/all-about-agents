{
  "schemaVersion": 1,
  "surface": "agy",
  "module": "emergency-guard",
  "event": "PreToolUse",
  "automatic": false,
  "probeRequired": true,
  "probe": {
    "status": "not run",
    "reason": "PreToolUse JSON input and deny output are documented, but hook-process failure behavior and portable package-root command resolution are not documented.",
    "manualSequence": [
      "Record the installed agy version and platform.",
      "Review the documented PreToolUse payload and package-root resolution.",
      "On a disposable package, perform an explicit probe before proposing an executable handler."
    ]
  },
  "contentRef": "core/hooks/emergency-guard.json",
  "failureMode": "unknown-disabled",
  "input": {
    "format": "json-object",
    "documentedFields": [
      "toolCall.name",
      "toolCall.args",
      "stepIdx"
    ]
  },
  "denyOutput": {
    "decision": "deny",
    "reason": "{{boundedReason}}"
  }
}
