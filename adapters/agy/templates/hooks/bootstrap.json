{
  "schemaVersion": 1,
  "surface": "agy",
  "module": "bootstrap",
  "automatic": false,
  "probeRequired": true,
  "probe": {
    "status": "not run",
    "reason": "PreInvocation JSON input and output are documented, but hook-process failure behavior and portable package-root command resolution are not documented.",
    "manualSequence": [
      "Record the installed agy version and platform.",
      "Review the current hook documentation and the exact installed plugin root.",
      "On a disposable package, probe command resolution, non-zero exit, timeout, malformed JSON, and first-invocation output before proposing an executable handler."
    ]
  },
  "contentRef": "core/skills/using-all-about-agents/SKILL.md",
  "failureMode": "unknown-disabled",
  "runtime": {
    "executable": "node",
    "minimumVersion": "22.12.0",
    "preflight": "required",
    "missingRuntime": "unavailable"
  },
  "input": {
    "format": "json-object",
    "fields": {
      "invocationNum": "integer",
      "initialNumSteps": "integer"
    }
  },
  "firstSessionWhen": {
    "invocationNum": 0,
    "initialNumSteps": 0
  },
  "firstSessionOutput": {
    "injectSteps": [
      {
        "ephemeralMessage": "{{canonicalContent}}"
      }
    ]
  },
  "otherSessionOutput": {
    "injectSteps": []
  }
}
