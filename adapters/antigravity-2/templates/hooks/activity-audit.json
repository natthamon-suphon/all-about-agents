{
  "schemaVersion": 1,
  "surface": "antigravity-2",
  "module": "activity-audit",
  "event": "PostToolUse",
  "optional": true,
  "automatic": false,
  "probeRequired": true,
  "probe": {
    "status": "not run",
    "reason": "The target Antigravity Desktop build does not have a verified hook command package-root resolution contract.",
    "manualSequence": [
      "Record the installed Antigravity Desktop version and platform.",
      "Review the documented PostToolUse payload and package-root resolution.",
      "On a disposable package, perform an explicit audit probe before proposing an executable handler."
    ]
  },
  "failureMode": "fail-open",
  "input": {
    "format": "json-object",
    "documentedFields": ["toolCall.name", "toolCall.args", "stepIdx"]
  },
  "recordedFields": ["timestamp", "surface", "actionId", "outcome", "sessionKeyHash"]
}
