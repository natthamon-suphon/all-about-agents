.worktrees/
.private-journal/
.claude/
.all-about-agents/
# Live Antigravity Desktop plugin slot; installed, not authored.
.agents/plugins/
.DS_Store
.idea/
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md). It is not part of the published
# plugin, so only the root checkout is ignored here.
/evals/
!/core/evals/
!/core/evals/**
.aaa/eval-runs/
tests/.tmp/
