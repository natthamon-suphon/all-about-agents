.worktrees/
.private-journal/
.claude/
.all-about-agents/
.DS_Store
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md). It is not part of the published
# plugin, so the whole directory is ignored here.
evals/
